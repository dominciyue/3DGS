# Core API (required)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9   # multipart/form-data for image uploads
pydantic>=2.6
python-dotenv>=1.0

# Agent LLM path (optional at runtime — MockPlanner is used if absent/no key)
anthropic>=0.39

# Optional image preprocessing (real mode only; mock mode doesn't need it)
Pillow>=10.0

# Tests
pytest>=8.0
httpx>=0.27               # required by fastapi.testclient
